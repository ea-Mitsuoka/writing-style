# CODEOWNERS — review routing. Later rules win. Personal account: single owner.
# Team syntax (@org/team) only resolves inside a GitHub Organization; on a personal
# account it silently disables CODEOWNERS, so every path routes to the owner here.
# See ai-dev-foundation's CODEOWNERS for the team-based reference layout.

*                       @ea-Mitsuoka
