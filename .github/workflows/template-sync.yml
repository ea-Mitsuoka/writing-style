# Distributes foundation updates to repos created from this template: opens a PR in the
# downstream repo whenever the template changes (files listed in .templatesyncignore are
# never touched).
#
# OPT-IN and safe by default: the job only runs when the repository variable
# TEMPLATE_SYNC_ENABLED is set to "true". So the template repo itself and any unmodified
# downstream repo never run it (no spurious scheduled failures). To enable sync in a
# downstream repo: set variable TEMPLATE_SYNC_ENABLED=true AND replace {{ORG}} below.
# A private direct parent additionally requires TEMPLATE_SYNC_SOURCE_AUTH=github-app,
# TEMPLATE_SYNC_SOURCE_APP_CLIENT_ID, and TEMPLATE_SYNC_SOURCE_APP_PRIVATE_KEY.
name: Template Sync

on:
  schedule:
    - cron: "17 7 * * *"   # daily, 07:17 UTC
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: template-sync-${{ github.repository }}
  cancel-in-progress: false

jobs:
  sync:
    if: vars.TEMPLATE_SYNC_ENABLED == 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          persist-credentials: false
      - name: Enforce one open Template Sync PR
        id: sync-preflight
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          open_pulls="$(
            gh pr list --repo "$GITHUB_REPOSITORY" --state open --limit 101 \
              --json number,headRefName,url
          )"
          if [[ "$(jq 'length' <<<"$open_pulls")" -gt 100 ]]; then
            echo "More than 100 open PRs prevent bounded Template Sync preflight" >&2
            exit 1
          fi
          open_sync_prs="$(
            jq '[.[] | select(.headRefName | startswith("chore/template_sync_")) | {number, url}]' \
              <<<"$open_pulls"
          )"
          open_count="$(jq 'length' <<<"$open_sync_prs")"
          if [[ "$open_count" -gt 1 ]]; then
            echo "Multiple open Template Sync PRs require human review before synchronization" >&2
            exit 1
          fi
          if [[ "$open_count" -eq 1 ]]; then
            existing_number="$(jq -r '.[0].number' <<<"$open_sync_prs")"
            existing_url="$(jq -r '.[0].url' <<<"$open_sync_prs")"
            echo "should_sync=false" >> "$GITHUB_OUTPUT"
            printf '### Template Sync deferred\n\nExisting PR [#%s](%s) remains the only open synchronization review.\n' \
              "$existing_number" "$existing_url" >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi
          echo "should_sync=true" >> "$GITHUB_OUTPUT"
      - name: Validate direct parent and source authentication
        if: steps.sync-preflight.outputs.should_sync == 'true'
        id: source-auth
        shell: bash
        env:
          SOURCE_AUTH: ${{ vars.TEMPLATE_SYNC_SOURCE_AUTH || 'public' }}
          HAS_SOURCE_APP_CLIENT_ID: ${{ vars.TEMPLATE_SYNC_SOURCE_APP_CLIENT_ID != '' }}
          HAS_SOURCE_APP_PRIVATE_KEY: ${{ secrets.TEMPLATE_SYNC_SOURCE_APP_PRIVATE_KEY != '' }}
          SOURCE_REPOSITORY: "{{ORG}}/ai-dev-foundation"
        run: |
          set -euo pipefail
          source_auth="$(python3 scripts/template_sync_auth.py \
            --root . \
            --source-repository "$SOURCE_REPOSITORY" \
            --mode "$SOURCE_AUTH" \
            --has-client-id "$HAS_SOURCE_APP_CLIENT_ID" \
            --has-private-key "$HAS_SOURCE_APP_PRIVATE_KEY")"
          echo "mode=$(jq -r .mode <<<"$source_auth")" >> "$GITHUB_OUTPUT"
          echo "owner=$(jq -r .owner <<<"$source_auth")" >> "$GITHUB_OUTPUT"
          echo "repository=$(jq -r .repository <<<"$source_auth")" >> "$GITHUB_OUTPUT"
      - name: Create direct-parent read token
        if: steps.source-auth.outputs.mode == 'github-app'
        id: source-app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3
        with:
          client-id: ${{ vars.TEMPLATE_SYNC_SOURCE_APP_CLIENT_ID }}
          private-key: ${{ secrets.TEMPLATE_SYNC_SOURCE_APP_PRIVATE_KEY }}
          owner: ${{ steps.source-auth.outputs.owner }}
          repositories: ${{ steps.source-auth.outputs.repository }}
          permission-contents: read
      - uses: AndreasAugustin/actions-template-sync@8a0f668b83c32a0f673353086d74f12b8853d4f5 # v2
        if: steps.sync-preflight.outputs.should_sync == 'true'
        id: template-sync
        with:
          # TEMPLATE: replace {{ORG}} with the org/user that owns the template repo.
          source_gh_token: ${{ steps.source-app-token.outputs.token || github.token }}
          target_gh_token: ${{ github.token }}
          source_repo_path: "{{ORG}}/ai-dev-foundation"
          upstream_branch: main
          pr_title: "build: sync with ai-dev-foundation template"
          pr_body: >-
            Direct-parent source commit will be recorded after synchronization. Before
            merge, review the accepted lock-to-source range, then finalize manual
            boundaries and .github/inheritance/lock.json in this same PR.
          pr_labels: "type:ci,type:dependencies"
          pr_commit_msg: "build: sync with ai-dev-foundation template"
      - name: Resolve exact direct-parent source commit
        if: steps.template-sync.outputs.pr_branch != ''
        id: source-provenance
        shell: bash
        env:
          GH_TOKEN: ${{ steps.source-app-token.outputs.token || github.token }}
          PR_BRANCH: ${{ steps.template-sync.outputs.pr_branch }}
          SOURCE_SHORT: ${{ steps.template-sync.outputs.template_git_hash }}
          SOURCE_REPOSITORY: "{{ORG}}/ai-dev-foundation"
        run: |
          set -euo pipefail
          if [[ -z "$SOURCE_SHORT" ]]; then
            SOURCE_SHORT="${PR_BRANCH##*_}"
          fi
          if [[ ! "$SOURCE_SHORT" =~ ^[0-9a-f]{7,40}$ ]]; then
            echo "Template Sync returned an invalid abbreviated source commit" >&2
            exit 1
          fi
          SOURCE_COMMIT="$(gh api "repos/${SOURCE_REPOSITORY}/commits/${SOURCE_SHORT}" --jq .sha)"
          if [[ ! "$SOURCE_COMMIT" =~ ^[0-9a-f]{40}$ || "$SOURCE_COMMIT" != "$SOURCE_SHORT"* ]]; then
            echo "Unable to expand the Template Sync source commit" >&2
            exit 1
          fi
          echo "commit=$SOURCE_COMMIT" >> "$GITHUB_OUTPUT"
      - name: Record exact direct-parent source commit
        if: steps.template-sync.outputs.pr_branch != ''
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          PR_NUMBER: ${{ steps.template-sync.outputs.pr_number }}
          PR_BRANCH: ${{ steps.template-sync.outputs.pr_branch }}
          SOURCE_COMMIT: ${{ steps.source-provenance.outputs.commit }}
          SOURCE_REPOSITORY: "{{ORG}}/ai-dev-foundation"
        run: |
          set -euo pipefail
          if [[ -z "$PR_NUMBER" ]]; then
            PR_NUMBER="$(gh pr view "$PR_BRANCH" --json number --jq .number)"
          fi
          if [[ ! "$PR_NUMBER" =~ ^[0-9]+$ ]]; then
            echo "Template Sync returned an invalid pull request number" >&2
            exit 1
          fi
          body="Direct-parent-source: https://github.com/${SOURCE_REPOSITORY}@${SOURCE_COMMIT}

          Before merge:
          - Review the accepted lock-to-source commit range.
          - Finalize manual boundaries and .github/inheritance/lock.json in this same reviewed PR."
          gh pr edit "$PR_NUMBER" --body "$body"
