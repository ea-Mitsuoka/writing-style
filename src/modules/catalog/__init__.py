"""Catalog — example bounded context (Clean Architecture layers + DDD).

Reference code to imitate (COD-050), not wired into the no-op template Makefile. See
MODULE.md for the contract. Delete this module when starting a real project, or copy its
shape.
"""
